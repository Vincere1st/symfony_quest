<h1>Article</h1>

<h2>Un nouvel article à été plublié</h2>

<table class="table">
    <tbody>
    <tr>
        <th>Id</th>
        <td>{{ article.id }}</td>
    </tr>
    <tr>
        <th>Title</th>
        <td>{{ article.title }}</td>
    </tr>
    <tr>
        <th>Content</th>
        <td>{{ article.content }}</td>
    </tr>
    <tr>
        <th>Voir l'article ici</th>
        <td><a href="http://localhost{{ path('article_show', {'id': article.id}) }}">show</a></td>
    </tr>
    </tbody>
</table>